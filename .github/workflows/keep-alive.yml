name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # প্রতি 10 মিনিটে
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render Server
        run: |
          curl -L --fail https://adobe-stock-license-server.onrender.com/ || true